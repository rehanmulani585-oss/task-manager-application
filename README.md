# STEP 1 — Create Folder

Open terminal:

```bash id="v5e7nf"
mkdir TaskManagerApplication
```

# STEP 2 — Open Folder

```bash id="z4r2pk"
cd TaskManagerApplication
```

# STEP 3 — Initialize Node Project

```bash id="y7x1md"
npm init -y
```

# STEP 4 — Install Packages

```bash id="a8q6tw"
npm install express mongoose cors dotenv
```

# STEP 5 — Open VS Code

```bash id="6m3vqa"
code .
```

# STEP 6 — Create Folder Structure

```text id="ec0r8u"
TaskManagerApplication
│
├── app.js
├── package.json
├── .env
├── models
│   └── Task.js
├── routes
│   └── taskRoutes.js
└── controllers
    └── taskController.js
```
